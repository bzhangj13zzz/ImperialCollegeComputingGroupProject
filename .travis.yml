language: java

stages:
  - test
  - build
  - name: deploy
    # require the branch name to be master
    if: branch = master
jobs:
  include:
    - stage: test
      script: # to be added
    - stage: build
      script: # to be added
    - stage: deploy
      script: # to be added